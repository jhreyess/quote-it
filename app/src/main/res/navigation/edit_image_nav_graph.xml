<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/edit_image_nav_graph"
    app:startDestination="@id/galleryChooserFragment">

    <fragment
        android:id="@+id/galleryChooserFragment"
        android:name="com.quoteit.android.ui.edit.GalleryChooserFragment"
        tools:layout="@layout/fragment_gallery"
        android:label="fragment_gallery">
        <action
            android:id="@+id/action_galleryChooserFragment_to_imagePreviewFragment"
            app:destination="@id/imagePreviewFragment" />
    </fragment>
    <fragment
        android:id="@+id/imagePreviewFragment"
        android:name="com.quoteit.android.ui.edit.ImagePreviewFragment"
        tools:layout="@layout/fragment_image_preview"
        android:label="fragment_image_preview"/>
</navigation>