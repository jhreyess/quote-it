<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.QuoteIt" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/yellow</item>
        <item name="colorOnPrimary">@color/darker_gray</item>

        <!-- Widgets -->
        <item name="colorSecondary">@color/yellow</item>
        <item name="colorOnSecondary">@color/darker_gray</item>

        <item name="errorTextColor">@color/coral</item>

        <!-- Cards background -->
        <item name="colorSurface">@color/lighter_gray</item>
        <item name="colorSurfaceVariant">@color/light_gray</item>

        <!-- Navbar inactive items -->
        <item name="colorOnSurface">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/darker_gray</item>
        <!-- Background color -->
        <item name="android:windowBackground" tools:targetApi="l">@color/darker_gray</item>
        <!-- Prefs -->
        <item name="bottomSheetDialogTheme">@style/Theme.BottomSheetDialog</item>
        <item name="bottomNavigationStyle">@style/Navbar</item>
    </style>

    <style name="Theme.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splashscreen</item>
        <item name="postSplashScreenTheme">@style/Theme.QuoteIt</item>
        <item name="windowSplashScreenAnimationDuration">2000</item>
    </style>

    <style name="Theme.BottomSheetDialog" parent="ThemeOverlay.MaterialComponents.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetModal</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>
</resources>